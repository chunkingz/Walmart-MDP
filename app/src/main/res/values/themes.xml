<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WalmartLogin" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WalmartLogin" parent="Base.Theme.WalmartLogin" />

    <style name="BlueButtonStyle">
        <item name="android:background">@drawable/blue_button_bg</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="WhiteButtonStyle">
        <item name="android:background">@drawable/white_button_bg</item>
        <item name="android:textColor">@color/blue</item>
    </style>

</resources>
